'use client';
import Link from 'next/link'; import { useRouter } from 'next/navigation'; import { FormEvent, useState } from 'react'; import { signInWithEmailAndPassword, sendPasswordResetEmail } from 'firebase/auth'; import { auth } from '@/lib/firebase'; import { PageShell } from '@/components/PageShell'; import { LogIn, Loader2 } from 'lucide-react';
export default function Login(){const router=useRouter();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [busy,setBusy]=useState(false);const [error,setError]=useState('');const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');try{const c=await signInWithEmailAndPassword(auth,email,password);if(!c.user.emailVerified){setError('Please verify your email before continuing.');setBusy(false);return;}router.push('/');}catch{setError('Email or password is incorrect.');setBusy(false)}};return <PageShell><div className="max-w-md mx-auto glass rounded-3xl p-7 sm:p-9"><div className="h-12 w-12 rounded-2xl bg-gradient-to-br from-violet-500 to-cyan-400 grid place-items-center"><LogIn/></div><h1 className="text-3xl font-black mt-6">Welcome back</h1><p className="text-slate-400 mt-1">Sign in to continue to 4uStream.</p><form onSubmit={submit} className="mt-7 space-y-4"><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" className="w-full glass rounded-xl px-4 py-3 outline-none"/><input required type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" className="w-full glass rounded-xl px-4 py-3 outline-none"/>{error&&<div className="text-sm text-red-300">{error}</div>}<button disabled={busy} className="w-full py-3 rounded-xl bg-white text-slate-950 font-extrabold flex items-center justify-center gap-2">{busy&&<Loader2 className="animate-spin" size={17}/>}Sign in</button></form><button onClick={async()=>{if(!email)return setError('Enter your email first.');try{await sendPasswordResetEmail(auth,email);setError('Password reset email sent.')}catch{setError('Could not send reset email.')}}} className="mt-4 text-xs text-violet-300">Forgot password?</button><p className="text-sm text-slate-500 mt-6">New here? <Link className="text-violet-300" href="/signup">Create an account</Link></p></div></PageShell>}
