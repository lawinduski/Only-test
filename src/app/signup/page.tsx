'use client';
import Link from 'next/link'; import { useRouter } from 'next/navigation'; import { FormEvent, useState } from 'react'; import { createUserWithEmailAndPassword, sendEmailVerification, updateProfile } from 'firebase/auth'; import { doc, serverTimestamp, setDoc } from 'firebase/firestore'; import { auth, db } from '@/lib/firebase'; import { PageShell } from '@/components/PageShell'; import { UserPlus, Loader2 } from 'lucide-react';
export default function Signup(){const router=useRouter();const [name,setName]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [busy,setBusy]=useState(false);const [error,setError]=useState('');const submit=async(e:FormEvent)=>{e.preventDefault();if(password.length<8)return setError('Password must be at least 8 characters.');setBusy(true);setError('');try{const c=await createUserWithEmailAndPassword(auth,email,password);await updateProfile(c.user,{displayName:name});await sendEmailVerification(c.user);await setDoc(doc(db,'users',c.user.uid),{uid:c.user.uid,name,email,status:'pending',createdAt:serverTimestamp()});router.push('/account');}catch(err:any){setError(err?.message?.includes('email-already')?'This email is already registered.':'Could not create the account.');setBusy(false)}};return <PageShell><div className="max-w-md mx-auto glass rounded-3xl p-7 sm:p-9"><div className="h-12 w-12 rounded-2xl bg-gradient-to-br from-violet-500 to-cyan-400 grid place-items-center"><UserPlus/></div><h1 className="text-3xl font-black mt-6">Create account</h1><p className="text-slate-400 mt-1">Your account starts as pending until an admin activates it.</p><form onSubmit={submit} className="mt-7 space-y-4"><input required value={name} onChange={e=>setName(e.target.value)} placeholder="Full name" className="w-full glass rounded-xl px-4 py-3 outline-none"/><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" className="w-full glass rounded-xl px-4 py-3 outline-none"/><input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password (8+ characters)" className="w-full glass rounded-xl px-4 py-3 outline-none"/>{error&&<div className="text-sm text-red-300">{error}</div>}<button disabled={busy} className="w-full py-3 rounded-xl bg-white text-slate-950 font-extrabold flex items-center justify-center gap-2">{busy&&<Loader2 className="animate-spin" size={17}/>}Create account</button></form><p className="text-sm text-slate-500 mt-6">Already registered? <Link className="text-violet-300" href="/login">Sign in</Link></p></div></PageShell>}
